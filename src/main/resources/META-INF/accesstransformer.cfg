# 26.1 client render/screen subsystem is ported without needing the old RenderType/RenderStateShard
# internals or AbstractContainerScreen#findSlot (the BER render-state pipeline and the screen
# extractBackground/extractLabels hooks need no access widening).

# Needed by FoundryControllerContainer#scrollTo (core menu, not client-only)
public-f net.minecraft.world.inventory.Slot y # y
